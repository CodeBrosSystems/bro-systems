---
// Hero con fondo tecnológico y animaciones
---
<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
  {/* Fondo tecnológico - Grilla de circuitos */}
  <div class="absolute inset-0 z-0 pointer-events-none">
    {/* Grid pattern */}
    <div class="grid-pattern absolute inset-0 opacity-20"></div>
    {/* Líneas de circuito animadas */}
    <div class="circuit-lines absolute inset-0">
      <div class="circuit-line line1"></div>
      <div class="circuit-line line2"></div>
      <div class="circuit-line line3"></div>
      <div class="circuit-line line4"></div>
      <div class="circuit-line line5"></div>
    </div>
    {/* Puntos de conexión */}
    <div class="nodes absolute inset-0">
      {[...Array(15)].map((_, i) => (
        <div class={`node absolute rounded-full bg-cyan-400 node${i+1}`}></div>
      ))}
    </div>
    {/* Código binario flotante */}
    <div class="binary-rain absolute inset-0 overflow-hidden opacity-10">
      {[...Array(20)].map((_, i) => (
        <div class={`binary-column absolute text-cyan-400 font-mono text-xs binary${i+1}`}>
          {Array(30).fill(0).map(() => Math.round(Math.random())).join('')}
        </div>
      ))}
    </div>
  </div>

  <div class="container relative z-10 flex flex-col items-center justify-center gap-12 pt-32 pb-16 md:pt-40 md:pb-24">
    {/* Sección de texto centrada */}
    <div class="max-w-3xl text-center animate-fadeInUp">
      <h1 class="main-title text-4xl md:text-6xl lg:text-7xl font-extrabold leading-tight mb-6">
        <span class="gradient-text bg-gradient-to-r from-white via-cyan-400 to-blue-500 bg-clip-text text-transparent">Soluciones digitales</span>
        <span class="gradient-text bg-gradient-to-r from-white via-cyan-400 to-blue-500 bg-clip-text text-transparent"> innovadoras para tu</span>
        <span class="gradient-text bg-gradient-to-r from-white via-cyan-400 to-blue-500 bg-clip-text text-transparent"> empresa</span>
      </h1>
      <p class="subtitle text-lg md:text-xl text-slate-300 font-light mb-8 animate-fadeInUp max-w-2xl mx-auto">
        Descubre nuestros sistemas web y aplicaciones personalizadas diseñados para optimizar tus operaciones y potenciar tu crecimiento.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-8 animate-fadeInUp">
        <a href="#servicios" class="btn btn-primary px-8 py-4 rounded-full font-bold text-lg shadow-lg bg-gradient-to-r from-blue-500 to-cyan-500 hover:from-blue-600 hover:to-cyan-600 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-cyan-400 ripple">Iniciar ahora</a>
        <a href="#contacto" class="btn btn-secondary px-8 py-4 rounded-full font-bold text-lg border-2 border-cyan-400 text-cyan-400 bg-transparent hover:bg-cyan-900/20 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-cyan-400 ripple">Contactar →</a>
      </div>
      <div class="flex flex-wrap gap-6 items-center justify-center animate-fadeInUp">
        <div class="flex items-center gap-2 text-slate-300 text-sm">
          <span class="w-3 h-3 rounded-full bg-green-400 animate-pulse"></span> 100% Personalizado
        </div>
        <div class="flex items-center gap-2 text-slate-300 text-sm">
          <span class="w-3 h-3 rounded-full bg-cyan-400 animate-pulse"></span> Código Limpio
        </div>
        <div class="flex items-center gap-2 text-slate-300 text-sm">
          <span class="w-3 h-3 rounded-full bg-purple-400 animate-pulse"></span> Tecnología Avanzada
        </div>
      </div>
    </div>
  </div>
  {/* Indicador de scroll */}
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 text-slate-400 text-center animate-bounce">
    <div>Descubre más</div>
    <div class="text-2xl mt-1">↓</div>
  </div>

  <style>
    /* Grid pattern tecnológico */
    .grid-pattern {
      background-image: 
        linear-gradient(rgba(34, 211, 238, 0.1) 1px, transparent 1px),
        linear-gradient(90deg, rgba(34, 211, 238, 0.1) 1px, transparent 1px);
      background-size: 50px 50px;
    }
    
    /* Líneas de circuito */
    .circuit-line {
      position: absolute;
      background: linear-gradient(90deg, transparent, rgba(34, 211, 238, 0.5), transparent);
      height: 2px;
    }
    .line1 { top: 20%; left: 0; width: 40%; animation: circuit 4s ease-in-out infinite; }
    .line2 { top: 40%; right: 0; width: 35%; animation: circuit 5s ease-in-out infinite 1s; }
    .line3 { top: 60%; left: 10%; width: 50%; animation: circuit 6s ease-in-out infinite 2s; }
    .line4 { top: 75%; right: 5%; width: 30%; animation: circuit 4.5s ease-in-out infinite 0.5s; }
    .line5 { top: 85%; left: 20%; width: 25%; animation: circuit 5.5s ease-in-out infinite 1.5s; }
    
    @keyframes circuit {
      0%, 100% { opacity: 0; transform: scaleX(0); }
      50% { opacity: 1; transform: scaleX(1); }
    }
    
    /* Nodos/puntos de conexión */
    .node { width: 6px; height: 6px; animation: nodePulse 2s ease-in-out infinite; }
    .node1 { top: 15%; left: 10%; animation-delay: 0s; }
    .node2 { top: 25%; left: 30%; animation-delay: 0.3s; }
    .node3 { top: 20%; right: 25%; animation-delay: 0.6s; }
    .node4 { top: 35%; left: 15%; animation-delay: 0.9s; }
    .node5 { top: 45%; right: 10%; animation-delay: 1.2s; }
    .node6 { top: 55%; left: 25%; animation-delay: 0.2s; }
    .node7 { top: 60%; right: 30%; animation-delay: 0.5s; }
    .node8 { top: 70%; left: 5%; animation-delay: 0.8s; }
    .node9 { top: 75%; right: 15%; animation-delay: 1.1s; }
    .node10 { top: 80%; left: 40%; animation-delay: 0.4s; }
    .node11 { top: 85%; right: 35%; animation-delay: 0.7s; }
    .node12 { top: 30%; left: 50%; animation-delay: 1s; }
    .node13 { top: 50%; left: 60%; animation-delay: 0.1s; }
    .node14 { top: 65%; left: 45%; animation-delay: 1.3s; }
    .node15 { top: 90%; left: 70%; animation-delay: 0.15s; }
    
    @keyframes nodePulse {
      0%, 100% { opacity: 0.3; transform: scale(1); box-shadow: 0 0 5px rgba(34, 211, 238, 0.5); }
      50% { opacity: 1; transform: scale(1.5); box-shadow: 0 0 15px rgba(34, 211, 238, 0.8); }
    }
    
    /* Código binario cayendo */
    .binary-column {
      writing-mode: vertical-rl;
      animation: binaryFall 15s linear infinite;
    }
    .binary1 { left: 5%; animation-delay: 0s; }
    .binary2 { left: 10%; animation-delay: 2s; }
    .binary3 { left: 15%; animation-delay: 4s; }
    .binary4 { left: 20%; animation-delay: 1s; }
    .binary5 { left: 25%; animation-delay: 3s; }
    .binary6 { left: 30%; animation-delay: 5s; }
    .binary7 { left: 35%; animation-delay: 0.5s; }
    .binary8 { left: 40%; animation-delay: 2.5s; }
    .binary9 { left: 45%; animation-delay: 4.5s; }
    .binary10 { left: 50%; animation-delay: 1.5s; }
    .binary11 { left: 55%; animation-delay: 3.5s; }
    .binary12 { left: 60%; animation-delay: 0.2s; }
    .binary13 { left: 65%; animation-delay: 2.2s; }
    .binary14 { left: 70%; animation-delay: 4.2s; }
    .binary15 { left: 75%; animation-delay: 1.2s; }
    .binary16 { left: 80%; animation-delay: 3.2s; }
    .binary17 { left: 85%; animation-delay: 5.2s; }
    .binary18 { left: 90%; animation-delay: 0.7s; }
    .binary19 { left: 95%; animation-delay: 2.7s; }
    .binary20 { left: 2%; animation-delay: 4.7s; }
    
    @keyframes binaryFall {
      0% { transform: translateY(-100%); }
      100% { transform: translateY(100vh); }
    }
    
    /* Animaciones de entrada */
    .animate-fadeInUp { animation: fadeInUp 1s ease-out both; }
    @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
  </style>
  <script is:inline>
    // Ripple en botones
    if (typeof window !== 'undefined') {
      document.querySelectorAll('.ripple').forEach(button => {
        button.addEventListener('click', function(e) {
          const ripple = document.createElement('span');
          const rect = this.getBoundingClientRect();
          const size = Math.max(rect.width, rect.height);
          const x = e.clientX - rect.left - size / 2;
          const y = e.clientY - rect.top - size / 2;
          ripple.style.cssText = `
            position: absolute;
            width: ${size}px;
            height: ${size}px;
            left: ${x}px;
            top: ${y}px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            transform: scale(0);
            animation: ripple 0.6s ease-out;
            pointer-events: none;
            z-index: 10;
          `;
          this.style.position = 'relative';
          this.style.overflow = 'hidden';
          this.appendChild(ripple);
          setTimeout(() => ripple.remove(), 600);
        });
      });
      // Keyframes para ripple
      const style = document.createElement('style');
      style.textContent = `@keyframes ripple { to { transform: scale(2); opacity: 0; } }`;
      document.head.appendChild(style);
    }
  </script>
</section> 